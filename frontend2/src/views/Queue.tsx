import PageTitle from "../components/elements/PageTitle";

const Queue: React.FC = () => {
  return (
    <div className="mb-20 ml-10 flex w-full flex-col">
      <PageTitle variant="bold">Recent Queue</PageTitle>
    </div>
  );
};

export default Queue;
