export type TourneyPageKey = "schedule" | "prizes" | "format" | "rules";

export enum TourneyPage {
  SCHEDULE = "schedule",
  PRIZES = "prizes",
  FORMAT = "format",
  RULES = "rules",
}

export type QuickStartedPageKey = "overview" | "creation" | "installation" | "resources" | "community";
export enum QuickStartedPage {
  OVERVIEW = "overview",
  ACCOUNT_AND_TEAM_CREATION = "creation",
  INSTALLATION = "installation",
  RESOURCES = "resources",
  JOIN_THE_COMMUNITY = "community"
}