{
  "name": "battlecode-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://play.battlecode.org",
  "dependencies": {
    "chartist": "^1.2.1",
    "duration": "^0.2.2",
    "jquery": "^3.6.0",
    "js-cookie": "^2.2.1",
    "moment": "^2.29.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-floater": "^0.8.2",
    "react-router": "^5.3.3",
    "react-router-dom": "^5.3.3",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "todo": "inserting build times apparently needs an external script, cuz of cross-platform problems. Well, i guess our deploy scripts are external scripts, so it's not like we're introducing new problems or smth. This script would need to generate .env.production.local on the fly, and use that?? include some links to docs about which one is used, https://create-react-app.dev/docs/adding-custom-environment-variables/ ",
    "start": "react-scripts start",
    "todo2": "the below command gets interpreted by powershell/cmd, which doesn't have a rm -rf, so this kinda doesn't even work lol",
    "build": "rm -rf build && react-scripts build",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
